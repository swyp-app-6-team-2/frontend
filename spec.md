# spec.md — 제품 스펙 (별따먹자)

> 무엇을 만드는가. 배경·구조는 [context.md](./context.md), 실행 계획은 [task.md](./task.md).
> **화면 픽셀 스펙의 최종 출처는 Figma 확정 디자인 `619:9650`**, 플로우 로직은 `499:406`.

## 1. 한 줄 정의 & 핵심 가치

레시피를 **여러 경로(URL·이미지·직접)로 쉽게 저장**하고, 냉장고 재료를 관리하며, **요리를 완료하면 ⭐별이 켜지는** 게이미피케이션으로 "요리 실행"을 동기부여하는 앱.

- 차별점: 저장이 아니라 **"해먹었다"는 완료 기록 → 별점등(별따먹자)**. 앱 이름이 곧 핵심 루프.
- AI가 영상/이미지/링크를 **자동으로 구조화된 레시피(요리명·재료·조리순서)**로 정리.

## 2. 두 축 & 탭

앱은 두 축으로 구성. 하단 탭바 **4개**: `홈 · 재료관리 · 나의레시피 · 마이`

- **재료 관리 (내 냉장고)** — 보유 재료, 유통기한 임박(D-1 등) 관리, 재료 추가(전체 533종 · 카테고리 필터)
- **나의 레시피** — 저장한 레시피 목록/검색/필터, 등록, 상세, 요리 완료 기록

## 3. 핵심 사용자 여정 (플로우차트 499:406 실측)

```
[나의레시피] 목록 → +등록 → 등록방법 선택
  ├ URL/영상 → AI 분석(로딩) → [인식 성공?] ─Y→ 내용 확인(AI 정리·수정) ─┐
  │                                        └─N→ URL 실패 → 직접 입력    │
  ├ 이미지/캡쳐 → OCR ────────────────────── [OCR 성공?] ─Y→ 내용 확인 ─┤→ [저장 슬롯 여유?]
  │                                                    └─N→ OCR 실패 → 직접 입력  │   ├─N→ 슬롯 초과 안내 → 확장
  └ 직접 입력 폼 ────[저장하기]──────────────────────────────────────┘   └─Y→ 저장 → 레시피 상세
레시피 상세 ─[요리 완료 버튼]→ ⭐요리 완료 기록(별 점등)
[재료관리] 내 냉장고 → 임박 재료 확인 → +재료 추가
```

**전이 규칙 (중요 — 헷갈리기 쉬움)**
- **"내용 확인"(AI 정리 결과)은 URL·이미지 경로 전용.** AI/OCR이 정리한 걸 확인·수정하는 화면. **직접 입력은 거치지 않고** 폼에서 바로 저장.
- **⭐요리 완료는 저장 직후가 아니다.** 저장 → 레시피 상세로 이동하고, ⭐는 상세 화면의 **"요리 완료" 액션**(성분보기·재료와 나란히)에서 뜬다.
- **저장 슬롯 한도(50) 체크는 저장 시점.** 여유 없으면 슬롯 초과 안내(23) → 확장.
- 실패(URL/OCR) 안내 → "직접 입력할게요"로 직접 입력 폼 전환.

> 화면 이름 세부는 확정 디자인 619:9650에서 최종 확인. 분기 구조는 플로우차트로 확정.

## 4. 화면 목록 (확정 플로우 카드 12~23)

| # | 화면 | 설명 | 라우트(stub) |
|---|---|---|---|
| — | 홈 | 별 진행도·추천·임박 요약 | `/home` |
| 12 | 내 냉장고 | 재료 목록·유통기한 임박 | `/fridge` |
| 13 | 재료 추가 | 검색 + 카테고리 필터 + 담기 | `/add-ingredient` |
| 14 | 나의 레시피 | 목록·검색·필터 | `/recipes` |
| 15 | 등록 방법 선택 | URL / 이미지 / 직접 | `/add-recipe` |
| 16 | URL로 등록 | 링크 입력 → AI | `/add-recipe-url` |
| 16-2 | 콘텐츠 확인 중 | AI 분석 로딩(영상·재료·순서) | `/add-recipe-loading` |
| 17 | 이미지로 등록 | 캡쳐 업로드 → OCR | `/add-recipe-image` |
| 18 | 직접 입력 | 이름·재료·조리순서 폼 | `/add-recipe-manual` |
| 20 | 내용 확인 | AI 정리 결과 확인·수정 → 저장 | `/recipe-detail` |
| 21 | 레시피 상세 | 저장된 레시피 + "해먹었어요" | `/recipe-view` |
| 22 | **요리 완료 기록** | ⭐별 점등(앱 정체성) | `/cook-complete` |
| — | 마이 | 슬롯 사용량·설정 | `/my` |
| — | 약관·정책 | 이용약관/개인정보/환불 | `/terms` |

**예외/엣지 화면**
| # | 화면 | 트리거 | 라우트 |
|---|---|---|---|
| 19-1 | URL 등록 실패 | 링크 분석 불가 → "직접 입력할게요" | `/url-failed` |
| 19-2 | 이미지(OCR) 실패 | 텍스트 인식 불가 → "직접 입력할게요" | `/ocr-failed` |
| 23 | 저장 슬롯 초과 | 50/50 도달 → "슬롯 확장하기" | `/slot-full` |

> 온보딩(요리 취향 선택)은 초기 탐색에 있었으나 확정 플로우엔 미포함 — 619:9650 확인 필요.

## 5. 데이터 모델 개요 (초안)

```
Recipe { id, name, category, tags[], servings, cookTime, source(url|image|manual|originUrl),
         ingredients: Ingredient[], steps: Step[], cookedCount, savedAt }
Ingredient { name, amount }        Step { order, text }
FridgeItem { name, category, expiryDate, quantity }
Tag { label }                       User { slotUsed, slotMax(=50), starCount }
```

- 등록 3경로 모두 최종적으로 `Recipe`로 수렴 (URL/이미지는 AI가 채우고, 직접은 수기).
- "해먹었어요" → `cookedCount++`, 별 점등 이벤트.
- 저장 슬롯 한도 50 (23번 화면).

## 6. MVP / 우선순위 (초안 — 플로우차트 "우선 개발 필요 화면"으로 확정 예정)

- **P0 (핵심 루프)**: 나의 레시피 목록 → 직접 입력 등록 → 내용 확인 → 저장 → 레시피 상세 → ⭐요리 완료
- **P1**: URL/이미지 등록 + AI/OCR 분석 + 실패 처리, 내 냉장고·재료 추가
- **P2**: 홈 대시보드, 마이/슬롯 확장, 필터/정렬 고도화, 온보딩

## 7. 비범위 (현재 논외)

거래/공유 마켓, 소셜, 결제(슬롯 확장 실제 결제), 다국어, 라이트 테마.

## 8. 열린 질문

- 홈과 내 냉장고의 관계(홈=냉장고인가, 별도 대시보드인가)
- 온보딩 유무·내용
- AI 분석의 실제 백엔드(자체 서버 vs 직접 LLM 호출) — 백엔드 팀 협의
- 4탭 아이콘 에셋
